# Chatbot
A simple React-based chatbot interface built with functional components and hooks. Includes a message display panel, input box, and dynamic state updates using useState. Demonstrates component communication, props handling, and clean UI structure using Vite + React.
